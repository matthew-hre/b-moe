{
  "name": "b-moe",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "linear:invite": "bun run src/scripts/generate-linear-invite-link.ts",
    "lint": "oxlint",
    "test": "bun test",
    "format": "oxfmt --write src"
  },
  "dependencies": {
    "awilix": "latest",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
